/* ══════════════════════════════════════════════════════════════
   順記燒臘館 · 設定檔

   ★★★  只有兩個值。第一個已經幫你填好了。  ★★★

   ══════════════════════════════════════════════════════════════ */


/* ──────────────────────────────────────────────────────────────
   ① COMMUNITY_URL　←　你的 LINE 社群（★ 必填，已填好）

   這是整個網站的「入口」：
     ・頂部的 LINE 按鈕     → 點了直接開這個社群
     ・按「＋」想點餐       → 一定要先加入這個社群
     ・送出訂單            → 訂單自動複製，帶客人回這個社群貼上

   換社群了才需要改。
   （LINE → 你的社群 → 右上角「⋯」→ 邀請 → 複製邀請連結）
   ────────────────────────────────────────────────────────────── */

var COMMUNITY_URL = 'https://line.me/ti/g2/GAhXwcx8SMs8nLxe-e7G0QSFVxCCYJmAx8rlMA?utm_source=invitation&utm_medium=link_copy&utm_campaign=default';


/* ──────────────────────────────────────────────────────────────
   ①-2　COMMUNITY_NAME　←　你的社群「顯示名稱」

   客人送出訂單時，畫面會叫他去點這個名字的聊天室。
   一定要跟 LINE 裡看到的名稱「一模一樣」，客人才找得到。
   ────────────────────────────────────────────────────────────── */

var COMMUNITY_NAME = '港式順記燒臘點餐區';


/* ──────────────────────────────────────────────────────────────
   ①-3　ORDER_IMAGE　←　訂單要不要做成「一張圖」

   true  = 客人按「送出訂單」→ 網頁把訂單畫成一張訂單表單（圖片）
           → 按綠色按鈕 → 選你的社群 → 按傳送 → 一張圖進群組
           （手機不支援分享圖片時，會自動改用文字訂單，不會卡住）

   false = 只用文字訂單（舊版行為）
   ────────────────────────────────────────────────────────────── */

var ORDER_IMAGE = true;


/* ──────────────────────────────────────────────────────────────
   ② LIFF_ID　←　選填。不填也能正常運作！
   ──────────────────────────────────────────────────────────────

   ★ 現在就能用：
     LIFF_ID 留空，網站照樣運作 —— 客人一定要加入社群才能點餐，
     訂單一樣會送到社群。你什麼都不用設定。

   ★ 填了會多什麼？
     加入社群之後，會再跳出一次「LINE 官方授權畫面」，
     客人按「允許」才能點餐。這樣你就能拿到他 **真實的 LINE 名稱**，
     訂單上會標出來，一看就知道是誰下的單，也擋得住亂填名字的人。

   ──────────────────────────────────────────────────────────────
   ⚠️ 不需要 LINE 官方帳號！
      用你自己平常在用的 LINE 帳號登入開發者後台就好，
      免費、不用營業登記、不用綁信用卡。
   ──────────────────────────────────────────────────────────────

   怎麼拿？手機就能做，約 5 分鐘：

   1. 打開 https://developers.line.biz/console/
      → 點【LINE 帳號登入】，用你自己的 LINE 掃碼登入

   2. Create a new provider → 名稱填「順記燒臘館」→ Create

   3. Create a new channel → 選【LINE Login】
        App types 一定要勾 ☑ Web app

      ※ 看到「Linked LINE Official Account」→ 留空 / 選 None

   4. 進到頻道 → 點【LIFF】頁籤 → Add
        LIFF app name : 順記點餐
        Size          : Full
        Endpoint URL  : https://wsad952701-beep.github.io/123/13/
                        ↑ 結尾的斜線 / 不能少！
        Scopes        : ☑ profile   ☑ openid    （兩個都要勾）
        Bot link      : Off    ← 這是官方帳號用的，關掉

      → Add

   5. 會出現一組 LIFF ID，長這樣：  2001234567-AbCdEfGh
      複製它，貼到下面的引號中間。

   6. ★ 把頻道從【Developing】改成【Published】
      （不改的話只有你自己登得進去，客人一律登入失敗！）
   ────────────────────────────────────────────────────────────── */

var LIFF_ID = '';
/*            ↑↑↑
      不填 → 只用社群當入口，現在就能上線。
      要填 → 貼在兩個引號中間，變成像這樣：

      var LIFF_ID = '2001234567-AbCdEfGh';
*/


/* ══════════════════════════════════════════════════════════════
   ③　GOOGLE 地圖資料
   ══════════════════════════════════════════════════════════════

   下面這些是從 Google 地圖抓下來的（2026/07 擷取）。
   店名、地址、電話、營業時間都已經核對過，跟你店裡的菜單海報一致。

   ★ 分數和評論數「會變」。
     過一陣子想更新，去 Google 地圖看一眼，改下面兩個數字就好，
     其他檔案都不用動。
   ────────────────────────────────────────────────────────────── */

var GOOGLE_PLACE_ID = 'ChIJvXlQ50QjaDQRBy1_aML9eFs';   /* 順記燒臘館在 Google 上的 ID */
var GOOGLE_LAT      = 24.9717817;
var GOOGLE_LNG      = 121.2356849;


/* ──────────────────────────────────────────────────────────────
   ③-2　要不要在網站上「秀出 Google 分數」？

   ⚠️ 先想一下再決定 ──
      你目前的分數是 3.5 顆星（88 則評論）。
      3.5 星秀在自己的網站上，對客人的第一印象「不一定加分」。
      分數養到 4.0 以上再打開，效果會好很多。

        true  = 網站的「門市」區塊會出現：★ 3.5 · 88 則 Google 評論
        false = 不顯示分數，但「Google 評價」按鈕還在，
                想看的客人自己點進去看（推薦先用這個）

   ※ 不管 true / false，Google 的地址、電話、營業時間、導航、地圖
     都一樣會顯示，不受影響。
   ────────────────────────────────────────────────────────────── */

var SHOW_GOOGLE_RATING = false;

var GOOGLE_RATING  = 3.5;          /* 星等 */
var GOOGLE_REVIEWS = 88;           /* 幾則評論 */
var GOOGLE_AS_OF   = '2026 / 07';  /* 什麼時候抓的 */


/* ══════════════════════════════════════════════════════════════
   ④　邀請客人留評價（★ 這是把 3.5 星拉起來最快的方法）
   ══════════════════════════════════════════════════════════════

   下面這個網址，客人點下去會「直接跳出 Google 的寫評價視窗」，
   不用先找到你的店、不用捲半天 —— 一鍵就能寫。

   ⚠️ 一件很重要的事（很多店家踩到這個雷）：

      不可以「只叫滿意的客人去評價」，也不可以「送飲料換好評」。
      這叫 review gating / 誘導好評，違反 Google 政策，
      被抓到會把你「整批評論刪掉」，甚至停權。

      要邀就「每一個客人都邀」，而且要說「不好也請直說」。
      這樣才安全，長期也才有用。
   ────────────────────────────────────────────────────────────── */

var GOOGLE_REVIEW_URL =
  'https://search.google.com/local/writereview?placeid=ChIJvXlQ50QjaDQRBy1_aML9eFs';


/* ──────────────────────────────────────────────────────────────
   ④-2　REVIEWS　←　要秀在網站上的客人評價

   ★ 預設是空的 → 網站上「客人怎麼說」那一整區【不會出現】。
     （寧可不放，也不要放假的）

   怎麼填：
     1. 手機打開 Google 地圖 → 搜尋「順記燒臘館」→ 點「評論」
     2. 挑幾則你喜歡的（五星的最好）
     3. 把【原文】整段複製起來，照下面的格式貼進來
        ※ 一定要用客人「真正寫的字」，不要自己編、不要改寫

     star : 幾顆星（1–5）
     name : 評論者在 Google 上顯示的名字
     when : 什麼時候評的（可留空）
     text : 評論原文

   填好之後存檔 → 上傳 config.js → 網站上就會出現。
   ────────────────────────────────────────────────────────────── */

var REVIEWS = [

  /*  ↓↓↓  把下面這段的  /*  和  *\/  拿掉，換成真的評論  ↓↓↓

  { star: 5, name: '王小明', when: '2026/06',
    text: '這裡貼 Google 上那則評論的原文，一個字都不要改。' },

  { star: 5, name: '陳美玲', when: '2026/05',
    text: '第二則評論的原文。' },

  */

];
